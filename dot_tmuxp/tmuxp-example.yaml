# set ~/.tmuxp/tmuxp-example.yaml
# tmuxp load tmuxp-example
session_name: tmuxp-example

windows:
  - window_name: workspace
    layout: tiled
    panes:
      - shell_command:
          - echo frontend
      - shell_command:
          - echo backend
      - shell_command: nvim